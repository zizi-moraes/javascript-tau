const names = ["Maria", "Carlos", "Marcelo"]

for (let i = 0; i < names.length; i++) {
  console.log(names[i]);
}

const numbers = [1, 2, 3, 4]

for (let j = 0; j < numbers.length; j++) {
  console.log(numbers[j] * 2);
}